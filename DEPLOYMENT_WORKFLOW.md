# 🎯 Deployment Workflow - Visual Guide

```
┌─────────────────────────────────────────────────────────────────┐
│                    DEPLOYMENT WORKFLOW                           │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────┐
│  1. LOCAL CODE  │
│   (Your PC)     │
└────────┬────────┘
         │ git push
         ▼
┌─────────────────┐
│   2. GITHUB     │
│  (Code Storage) │
└────┬───────┬────┘
     │       │
     │       │ Connect repo
     │       ▼
     │  ┌──────────────────────┐
     │  │   3. VERCEL          │
     │  │   (Frontend)         │
     │  │                      │
     │  │ • Builds React app   │
     │  │ • Serves static      │
     │  │   files (HTML/CSS/JS)│
     │  │                      │
     │  │ URL:                 │
     │  │ your-app.vercel.app  │
     │  └──────────┬───────────┘
     │             │
     │             │ API Calls (HTTP/WebSocket)
     │             │ VITE_API_URL environment variable
     │             ▼
     │  ┌──────────────────────┐
     │  │   4. RENDER          │
     │  │   (Backend)          │
     └─►│                      │
 Connect │ • Runs Node.js       │
  repo   │ • Express server     │
        │ • Socket.io          │
        │ • API endpoints      │
        │                      │
        │ URL:                 │
        │ your-backend.        │
        │ onrender.com         │
        └──────────┬───────────┘
                   │
                   │ Database queries
                   │ MONGODB_URI environment variable
                   ▼
        ┌──────────────────────┐
        │  5. MONGODB ATLAS    │
        │   (Database)         │
        │                      │
        │ • Stores all data    │
        │ • Users, posts, etc. │
        │                      │
        │ Already configured ✓ │
        └──────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                     ENVIRONMENT VARIABLES                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  VERCEL (Frontend):                                              │
│  └─ VITE_API_URL=https://your-backend.onrender.com             │
│                                                                  │
│  RENDER (Backend):                                               │
│  ├─ FRONTEND_ORIGIN=https://your-app.vercel.app                │
│  ├─ MONGODB_URI=mongodb+srv://...                              │
│  ├─ JWT_ACCESS_SECRET=secure_secret_here                       │
│  ├─ JWT_REFRESH_SECRET=another_secure_secret                   │
│  └─ NODE_ENV=production                                         │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    USER INTERACTION FLOW                         │
└─────────────────────────────────────────────────────────────────┘

 User Browser
      │
      │ Visits https://your-app.vercel.app
      ▼
┌─────────────┐
│   VERCEL    │ Serves HTML, CSS, JavaScript
│  (Frontend) │ React app loads in browser
└─────┬───────┘
      │
      │ User clicks "Login" → React makes API call
      │ fetch('https://your-backend.onrender.com/api/v1/auth/login')
      ▼
┌─────────────┐
│   RENDER    │ Receives request
│  (Backend)  │ Checks credentials in MongoDB
└─────┬───────┘ Sends back JWT token
      │
      │ Query database
      ▼
┌─────────────┐
│   MONGODB   │ Returns user data
│   ATLAS     │
└─────────────┘
      │
      │ Backend sends response
      ▼
┌─────────────┐
│   VERCEL    │ React updates UI
│  (Frontend) │ User is logged in
└─────────────┘
      │
      │ WebSocket connection established
      │ wss://your-backend.onrender.com
      ▼
┌─────────────┐
│   RENDER    │ Real-time updates
│  (Backend)  │ (messages, notifications)
└─────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                       COST BREAKDOWN                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ✅ Vercel (Frontend):        FREE                              │
│     • 100 GB bandwidth/month                                     │
│     • Unlimited deployments                                      │
│     • Always on                                                  │
│                                                                  │
│  ⚠️  Render (Backend):        FREE (with limitations)           │
│     • 750 hours/month                                            │
│     • Spins down after 15 min inactivity                         │
│     • First request takes 30-60 seconds to wake up               │
│     • 512 MB RAM                                                 │
│                                                                  │
│  ✅ MongoDB Atlas:            FREE                              │
│     • 512 MB storage                                             │
│     • Shared cluster                                             │
│                                                                  │
│  💰 TOTAL: $0/month                                              │
│                                                                  │
│  Want always-on backend? Upgrade Render to $7/month             │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    DEPLOYMENT TIMELINE                           │
└─────────────────────────────────────────────────────────────────┘

Minute 0:  Push code to GitHub                    [████] 2 min
Minute 2:  Create Render web service              [██] 1 min
Minute 3:  Render builds & deploys backend        [████████] 5-8 min
Minute 11: Create Vercel project                  [██] 1 min
Minute 12: Vercel builds & deploys frontend       [████] 3-4 min
Minute 16: Update Render FRONTEND_ORIGIN          [██] 1 min
Minute 17: Test deployment                        [██] 2 min
─────────────────────────────────────────────────────────────────
Minute 19: ✅ YOUR APP IS LIVE!

Total time: ~15-20 minutes
